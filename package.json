{
  "name": "@aurelienbbn/harness",
  "version": "0.0.0",
  "private": true,
  "description": "Shared quality tooling for Aurelien projects.",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "pnpm --recursive --if-present build",
    "check": "pnpm build && pnpm lint && pnpm fmt:check && pnpm test",
    "lint": "vp lint",
    "fmt": "vp fmt",
    "fmt:check": "vp fmt --check",
    "test": "pnpm --recursive --if-present test",
    "test:watch": "pnpm --recursive --if-present test:watch",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "2.31.0",
    "@voidzero-dev/vite-plus-core": "0.2.1",
    "oxfmt": "0.56.0",
    "oxlint": "1.71.0",
    "tsdown": "0.22.3",
    "typescript": "6.0.3",
    "vite-plus": "0.2.1"
  },
  "packageManager": "pnpm@11.9.0"
}
