# Attributions: install-harness

Ideas this skill re-implements in its own words. Never loaded by agents; kept for `rg "@attribution"` and the repository CREDITS.md.

```text
@attribution https://github.com/TanStack/intent (MIT; inspiration: a task → skill map in always-loaded agent instructions)
```
