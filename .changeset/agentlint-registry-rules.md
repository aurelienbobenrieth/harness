---
"@aurelienbbn/agentlint-plugin-shopify-theme": minor
---

Add registry-drift (delivery modules must exist in the primitive registry) and schema-ux (settings schemas need t: labels and header grouping) review rules; schema-ux runs on config/settings_schema.json via agentlint's new JSON grammar.
