---
"@aurelienbbn/oxlint-plugin-effect": major
---

Remove rules now owned by the official type-aware `@effect/tsgo` Oxlint diagnostics: ambient nondeterminism, cascading and nested Layer provision, floating Effects, plain `yield`, raw JSON parsing and serialization, unsafe error channels and Effect assertions, identifier matching, `Effect.fn` opportunities, missing `return yield*`, and root-import policy.
