---
"@aurelienbbn/conformance-shopify-app": minor
---

Match reviewed Shopify CLI app manifest names, extension directory depth, empty-list defaults and alternate extension manifest basenames. Ignore backup-style app filenames during discovery and reject them as explicit selectors. Check effective per-extension API versions, including entry overrides. Restrict function localization and checkout budgets to actual extension declarations so unrelated settings cannot activate another surface's checks.
