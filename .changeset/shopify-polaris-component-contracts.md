---
"@aurelienbbn/oxlint-plugin-shopify-app": minor
---

Add ten Shopify AST rules for Polaris action/form/loading names, action slot contracts, hidden page asides, ignored button form behavior, tooltip interactivity, money-field copy, and scoped REST Admin migration. Resolve known empty modal headings and inline spread overrides. Keep runtime translations, custom component output, SDK wrappers and external eligibility outside static proof.

Link the tested App Home recipe and require explicit category and source-path selection for optional API and checkout cancellation policies.
