---
"@aurelienbbn/conformance-core": patch
---

Correct diagnostic documentation links to the canonical repository and validate them in the catalog gate. Refresh original operating skills with scoped invocation, preserved authorization, precise evidence, and readable communication; add skill structure and independent behavior evaluations. Fix the design-system example to use an explicit installed CSS builder.

Core conformance's Vitest adapter now skips unavailable optional evidence and fails failed evaluations independently of warning severity. Evaluated advisory warnings still pass. A real Vitest subprocess regression verifies unavailable, malformed, advisory, and required-tool outcomes.
