---
"@aurelienbbn/oxlint-plugin-core": minor
---

Add `padding-before-exit`, which requires a blank line above a `return` or `throw` that follows another statement in the same list and inserts it with an autofix.
