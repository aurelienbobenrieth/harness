---
"@aurelienbbn/agentlint-plugin-shopify-app": minor
---

Add server-side reviews: `admin-api-loop-review` (Admin GraphQL calls in loops and iteration callbacks), `webhook-handler-review` (one review per webhook handler file), and the opt-in change rule `scope-change-review` (access scopes added to an app manifest). Add `appServerPreset`. Extend `checkout-network-discipline` with the checkout capabilities security checks and `session-token-auth` with expiring offline token guidance; both standards move to revision 2.
