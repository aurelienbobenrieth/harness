---
"@aurelienbbn/oxlint-plugin-effect": minor
---

Add `padding-after-dependencies`, which requires a blank line between the leading service dependencies of an Effect generator body and the logic below them and inserts it with an autofix.
