---
"@aurelienbbn/oio": minor
---

New Theme OS CLI (`oio`), Effect TS mirroring the agentlint architecture: `registry check`/`registry sync` (markdown table <-> registry.json with preserved surface/delivery classification), `surface audit` (merchant catalog + unclassified detection), `budget` (asset size budgets), and `scaffold block|snippet|section|enhancer|machine <id>` (LiquidDoc/schema/test stubs with registry status flip). Configured via `oio.config.ts`.
