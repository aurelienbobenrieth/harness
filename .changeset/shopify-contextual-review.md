---
"@aurelienbbn/agentlint-plugin-shopify-app": minor
---

Add scoped Shopify component and copy reviews for banner persistence, field recovery, destructive actions, modal workflows, ambiguous action labels, and review requests. Add an opt-in route and extension review factory with explicit applicability and runtime evidence requirements. Findings schedule review and do not assert platform eligibility.

Recognize direct Save Bar attributes instead of incidental prop text. Reset configured authentication and checkout regex state, update ID token references, and correct stale checkout guidance to include `shopify.query()` and initial data loading before first paint. Keep all additions within the existing private agentlint draft boundary.
