---
"@aurelienbbn/oxlint-plugin-effect": minor
---

Initial release. 32 oxlint rules for Effect code: runtime boundaries, failure channels, concurrency, services, Schema. Only what `@effect/tsgo` doesn't own.
