---
"@aurelienbbn/conformance-shopify-app": minor
---

Add HTTPS app URL, webhook subscription, quarterly API version, and opt-in Built for Shopify extension prerequisite checks. Follow selected deployment extension directories, require App Bridge as the first script in every selected document head, and reject checkout budgets that weaken the documented limit. Category presence and declared transport remain static prerequisites; live behavior and Shopify eligibility require separate evidence.

Provide a supplied-response iframe policy evaluator and isolated handler probes for privacy webhook HMAC rejection, with a valid signed control so broken routes cannot appear authenticated.
