---
"@aurelienbbn/conformance-core": minor
"@aurelienbbn/conformance-shopify-app": minor
"@aurelienbbn/oxlint-plugin-core": minor
---

Add JSON write plans and dry runs to all mutating oio commands, snapshot input hashes, serialize cooperating writers, and reject observed concurrent edits. Generate DOM preservation tests with an explicit jsdom environment and escape/validate generated event documentation.

Expose core conformance coverage status separately from violations. Preserve explicit skips in reports and Vitest output; reject malformed tool reports, invalid duplication budgets, and unknown skip IDs.

Support explicit Shopify app deployment manifests consistently across webhook and App Bridge checks. Require compliance topics to belong to subscriptions with delivery URIs.

Recognize same-file export aliases/default identifiers and public overloads in object-return policy. Resolve aliased, namespace, shadowed, and parameterized Vitest APIs; permit explicitly configured assertion helpers.

These changes can introduce diagnostics for previously missed syntax or invalid inputs. Package READMEs include migration examples and supported boundaries. Packed consumer acceptance now invokes all lint engines, compiles public API uses and expected type errors, registers conformance suites, and executes every scaffold kind without publishing.
